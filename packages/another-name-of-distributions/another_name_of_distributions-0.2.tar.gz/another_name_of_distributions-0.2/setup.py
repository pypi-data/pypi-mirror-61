from setuptools import setup

setup(name='another_name_of_distributions',
      version='0.2',
      description='Gaussian distributions',
      packages=['another_name_of_distributions'],
      zip_safe=False)
