from setuptools import setup

setup(name='distributions_udacity_test',
      version='0.1.1',
      description='Gaussian distributions',
      packages=['distributions_udacity_test'],
      zip_safe=False)
