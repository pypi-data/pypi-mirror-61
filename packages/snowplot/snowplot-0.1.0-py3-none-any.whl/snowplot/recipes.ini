[data_recipe]
lyte_probe_trigger:
  has_section = lyte_probe
smp_trigger:
  has_section = snow_micropen
plotting:
  apply_defaults = True
labeling:
  apply_defaults = True
output:
  apply_defaults = True

[lyte_probe_recipe]
lyte_probe_trigger:
  has_section = lyte_probe
lyte_probe:
  apply_defaults = True

[snow_micropen_recipe]
snow_micropen_trigger:
  has_section = snow_micropen
snow_micropen:
  apply_defaults = True
