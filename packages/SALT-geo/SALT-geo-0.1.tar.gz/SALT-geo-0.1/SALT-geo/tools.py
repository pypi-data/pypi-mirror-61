#!/usr/bin/env python3# -*- coding: utf-8 -*-"""Basic helper tools"""def SayHello():    print('Hello, World!')    def extractdatetime(FILE_NAME, FILE_TYPE):        """Extract date and time from input filename and returns string in YYYYMMDD"""        #check filetype and file_name are strings. if not return exception and default response                #for specific type of file, do the thing        if FILE_TYPE.casefold() == 'tdt':            print(FILE_TYPE.casefold())        elif FILE_TYPE.casefold() == 'icesat2':            print(FILE_TYPE.casefold())        else:            print('Unsopported filetype. Currently supports TDT, WV')def ICESAT2_traceback():    """Creates a unique identifier for a single icesat 2 return to trade back to source data"""    #format: product_granule_sensor_FID maybe include date?    print('Hello WOrld')        