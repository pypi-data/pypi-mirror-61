from .utils import print_err


def init() -> None:
    print_err("Git sync is currently not implemented, but we saved your setting")
    print_err("and will activate sync once it's ready.")
    print_err()
    print_err()
