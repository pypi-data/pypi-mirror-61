C     -*- fortran -*-
C     This file is autogenerated with f2py (version:2)
C     It contains Fortran 77 wrappers to fortran functions.

      subroutine f2pywraparclen (arclenf2pywrap, p, q)
      external arclen
      real p(3)
      real q(3)
      real arclenf2pywrap, arclen
      arclenf2pywrap = arclen(p, q)
      end


      subroutine f2pywrapsig0 (sig0f2pywrap, n1, n2, n, x, y, z, h
     &, list, lptr, lend, grad, iflgb, hbnd, tol, iflgs, sigma, ier)
      external sig0
      integer n1
      integer n2
      integer n
      integer iflgb
      real hbnd
      real tol
      integer iflgs
      integer ier
      real x(n)
      real y(n)
      real z(n)
      real h(n)
      integer list(6 * n - 12)
      integer lptr(6 * n - 12)
      integer lend(n)
      real grad(3,n)
      real sigma(6 * n - 12)
      real sig0f2pywrap, sig0
      sig0f2pywrap = sig0(n1, n2, n, x, y, z, h, list, lptr, lend,
     & grad, iflgb, hbnd, tol, iflgs, sigma, ier)
      end


      subroutine f2pywrapsig1 (sig1f2pywrap, n1, n2, n, x, y, z, h
     &, list, lptr, lend, grad, iflgb, hpbnd, tol, iflgs, sigma, ier)
      external sig1
      integer n1
      integer n2
      integer n
      integer iflgb
      real hpbnd
      real tol
      integer iflgs
      integer ier
      real x(n)
      real y(n)
      real z(n)
      real h(n)
      integer list(6 * n - 12)
      integer lptr(6 * n - 12)
      integer lend(n)
      real grad(3,n)
      real sigma(6 * n - 12)
      real sig1f2pywrap, sig1
      sig1f2pywrap = sig1(n1, n2, n, x, y, z, h, list, lptr, lend,
     & grad, iflgb, hpbnd, tol, iflgs, sigma, ier)
      end


      subroutine f2pywrapsig2 (sig2f2pywrap, n1, n2, n, x, y, z, h
     &, list, lptr, lend, grad, tol, iflgs, sigma, ier)
      external sig2
      integer n1
      integer n2
      integer n
      real tol
      integer iflgs
      integer ier
      real x(n)
      real y(n)
      real z(n)
      real h(n)
      integer list(6 * n - 12)
      integer lptr(6 * n - 12)
      integer lend(n)
      real grad(3,n)
      real sigma(6 * n - 12)
      real sig2f2pywrap, sig2
      sig2f2pywrap = sig2(n1, n2, n, x, y, z, h, list, lptr, lend,
     & grad, tol, iflgs, sigma, ier)
      end

