from setuptools import setup

setup(name='distributions_kamal',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_kamal'],
      author = 'Kamal Jain',
      zip_safe=False)
